version: "3.8"

services:
  metrics-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grafana-cloud-proxy
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
